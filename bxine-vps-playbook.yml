---
- hosts: bxine
  remote_user: ***REMOVED*** #root
  become: true
  vars:
    server_ip: "{{ bxine_ip }}"
    dropbox_folder: /home/***REMOVED***/Dropbox
  vars_files:
    - './secrets/users_secrets.yml'

  roles:
    - { role: base_packages, tags: base }
    - { role: nginx, tags: ngnix, mina_deploy: true }
    - { role: create_users, users: "{{ server_users }}" }
    - { role: development_user, users: "{{ server_users }}"}
    - { role: python, tags: python }
    - { role: dropbox, tags: dropbox }
    - { role: calibre-web, tags: calibre, calibre_name: alexandrie, calibre_port: 8085 }
    - { role: calibre-web, tags: calibre, calibre_name: bazar, calibre_port: 8086 }
    - { role: calibre-web, tags: calibre, calibre_name: comics, calibre_port: 8087 }
    - { role: munin-node, munin_master_ip: "{{ axine_ip }}" }
