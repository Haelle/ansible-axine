---
- hosts: axine-vps
  remote_user: root
  become: true
  vars_files:
    - './secrets/users_secrets.yml'

  roles:
    - { role: base_packages }
    - { role: development_user, users: "{{ server_users }}"}

