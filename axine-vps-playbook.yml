---
- hosts: axine-vps
  remote_user: root
  become: true
  vars_files:
    - './secrets/users_secrets.yml'
  vars:
    http_port: 80
    max_clients: 200
    axine_users:
      - ***REMOVED***
      - line

  roles:
    #- { role: base_packages, user: "***REMOVED***" }
    - { role: development_user, usernames: "{{ axine_users }}"}

